// Base dark/accent palette for the leadership-visual-essay format — keep the
// same palette across videos for series continuity (see the skill's
// "Structure conventions for this format"). A video can override any of
// these in its own layout.ts if the essay genuinely calls for a different
// palette, but that should be a deliberate exception, not the default.
export const BG = "#15100F";
export const TEXT = "#D4D3D2";
// Text has one neutral color. Hierarchy comes from size, weight, spacing,
// and opacity — never from switching between white and grey.
export const DIM_TEXT = TEXT;
export const ACCENT = "#EEA530";
// Geometry may recede through opacity and stroke width, but remains in the
// same neutral grey as the rest of the explanatory case-study layer.
export const LINE_INACTIVE = TEXT;
export const LINE_ACTIVE = TEXT;

export const FONT = '"Helvetica Neue", Arial, sans-serif';
